Choose a concrete, implementable access-control scheme that satisfies the capabilities matrix as well as possible (including how the plan can still be advanced via `aap-*` when intended), and specify exactly what changes are required in the generator/AGENTS.md, `start_codex`/session setup, `remountd` integration (if used), and the `aap-*` scripts. This design should be decision-complete: it should be obvious how to implement and how to verify success.
