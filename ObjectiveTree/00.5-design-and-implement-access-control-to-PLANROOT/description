Define and implement access control for $PLANROOT so the coder can update plan progress via aap-* without being able to edit plan structure or descriptions, and without triggering “work around read-only” behavior. Target design: ObjectiveTree structure+descriptions read-only for coder; only the minimal progress state writable (status files and the current_objective pointer), with scripts as the only supported mutation path and clear error messages on forbidden actions.
